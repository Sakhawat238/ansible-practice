- name: install unzip
  package:
    name: unzip

- name: install terraform
  unarchive:
    src: https://.......terraform_0.12.28_linux_amd64.zip
    dest: /usr/local/bin
    remote_src: yes
    owner: root
    group: root
    mode: 0755